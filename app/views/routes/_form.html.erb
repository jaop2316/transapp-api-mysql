<%= form_with(model: route, local: true) do |form| %>
  <% if route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(route.errors.count, "error") %> prohibited this route from being saved:</h2>

      <ul>
      <% route.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :codRuta %>
    <%= form.text_field :codRuta, id: :route_codRuta %>
  </div>

  <div class="field">
    <%= form.label :nomRuta %>
    <%= form.text_field :nomRuta, id: :route_nomRuta %>
  </div>

  <div class="field">
    <%= form.label :numBuses %>
    <%= form.number_field :numBuses, id: :route_numBuses %>
  </div>

  <div class="field">
    <%= form.label :observRuta %>
    <%= form.text_field :observRuta, id: :route_observRuta %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
